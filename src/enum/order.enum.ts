export enum PaymentMethod {
  COD = "cod",
}

export enum OrderStatus {
  PROCESSING = "processing",
  PLACED = "placed",
  DELIVERED = "delivered",
  CANCELED = "canceled",
  REFUNDED = "refunded",
}